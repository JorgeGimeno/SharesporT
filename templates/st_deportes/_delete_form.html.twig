

<form method="post" action="{{ path('st_deportes_delete', {'id': st_deporte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ st_deporte.id) }}">
    <button class="btn">Delete</button>
</form>
