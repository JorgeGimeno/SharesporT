<form method="post" action="{{ path('st_reacciones_delete', {'id': st_reaccione.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ st_reaccione.id) }}">
    <button class="btn">Delete</button>
</form>
